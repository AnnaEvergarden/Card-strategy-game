# 代码结构与方法索引（Xmind 用）

> 基于 `Assets/Scripts` 下全部 `.cs` 文件整理（2026-03-22）。  
> **用途**：按「文件夹 → 文件 → 类型 → 方法」在 Xmind 中建脑图；第三节为纯缩进大纲，可直接粘贴到支持大纲导入的工具。  
> **说明**：下列「方法」含实例方法、静态方法、**构造函数**；不含字段、事件、`const`、仅带 `=>` 的简单属性（若需可再补一版「完整成员」）。  
> **可选**：若本机已安装 Python 3，可运行 `Documentation/_gen_cs_outline.py` 尝试自动生成（需自行校验输出）。

---

## 1. 文件目录树（`Assets/Scripts` 相对路径）

```
Common/
  Singleton.cs
Core/
  Battle/
    BattleContext.cs
    BattleFieldView.cs
    BattleManager.cs
    CardUnit.cs
  Card/
    CardData.cs
    CardEffect.cs
    CardInstance.cs
    CardStatsService.cs
    CardUI.cs
Services/
  AccountEntry.cs
  AccountManager.cs
  CardDataBase.cs
  SaveData.cs
  SaveManager.cs
  UIPanelManager.cs
  SceneManagers/
    BattleSceneManager.cs
    TitleSceneManager.cs
Test/
  TestBattleScene.cs
UI/
  BaseUIPanel.cs
  BattleScene/
    BattlePanel.cs
    CardActionPanel.cs
    CardSelectItemView.cs
    CardSelectPanel.cs
  TitleScene/
    LoginPanel.cs
    SaveSelectPanel.cs
    TitlePanel.cs
```

---

## 2. 按文件：类型与方法

### `Common/Singleton.cs`

- **`Singleton<T>`**（`where T : MonoBehaviour`）
  - `Awake()`（`protected virtual`）
  - `OnDestroy()`（`protected virtual`）

---

### `Test/TestBattleScene.cs`

- **`TestBattleScene`**
  - `Start()`（`private`）
  - `OnClickStartBattleButton()`（`private`）

---

### `Services/AccountEntry.cs`

- **`AccountEntry`**
  - `AccountEntry()`（无参构造）
  - `AccountEntry(string _userName, string _password)`（构造）
- **`AccountListData`**
  - （无显式方法，仅字段 `accounts`）

---

### `Services/SaveManager.cs`

- **`SaveManager`**
  - `GetSavePath(int? _slotIndex = null)`（`private`）
  - `Awake()`（`protected override`）
  - `Load()`
  - `Save()`
  - `AddCardToInventory(CardInstance _instance)`
  - `FindInstanceById(string _id)`
  - `GetCurrentDeck()`
  - `SetCurrentDeckIndex(int index)`
  - `AddDeck(string _deckName)`
  - `RemoveDeckAt(int _index)`
  - `SetDeckInstanceIds(int _deckIndex, List<string> _instanceIds)`
  - `CreateDefaultSaveData()`（`private static`）
  - `SelectSlot(int _slotIndex)`
  - `HasSaveInSlot(int _index)`
  - `GetSlotOccupied()`
  - `GetCurrentSlotIndex()`
  - `GetCurrentAccountSaveCount()`
  - `CreateDefaultSaveInSlot(int _slotIndex)`

---

### `Services/SaveData.cs`

- **`SaveData`**
  - `SaveData()`（无参构造）
- **`DeckSlot`**
  - `DeckSlot()`（无参构造）
  - `DeckSlot(string _name)`（构造）

---

### `Services/AccountManager.cs`

- **`AccountManager`**
  - `Awake()`（`protected override`）
  - `LoadAccounts()`（`private`）
  - `SaveAccounts()`（`private`）
  - `Register(string _userName, string _password)`
  - `Login(string _userName, string _password)`
  - `Logout()`
  - `GetCurrentAccountId()`
  - `GetAccountNum()`
  - `IsLoggedIn()`

---

### `Services/CardDataBase.cs`

- **`CardDataBase`**
  - `Awake()`（`protected override`）
  - `GetCardData(string _cardId)`
  - `RegisterCardData(CardData _data)`

---

### `Services/UIPanelManager.cs`

- **`UIPanelManager`**
  - `RegisterPanel(string _panelName, BaseUIPanel _panel)`
  - `UnRegisterPanel(string _panelName, BaseUIPanel _panel)`
  - `ShowPanel(string _panelName)`
  - `HideAllPanels()`（`private`）
  - `PushPanel(string _panelName)`
  - `Back()`
  - `ReplacePanel(string _panelName)`
  - `ClearStackAndShow(string _panelName)`
  - `GetPanel(string _panelName)`

---

### `Services/SceneManagers/TitleSceneManager.cs`

- **`TitleSceneManager`**
  - `Start()`（`private`）

---

### `Services/SceneManagers/BattleSceneManager.cs`

- **`BattleSceneManager`**
  - `Start()`（`private`）

---

### `Core/Battle/BattleManager.cs`

- **`BattleSide`**（`enum`）
  - `Player1`
  - `Player2`
- **`BattleManager`**
  - `StartBattlePrepare(BattleContext _ctx)`
  - `ConfirimPLayer1Actives(int _idxA, int _idxB)`
  - `AutoSelectPlayer2Actives()`（`private`）
  - `StartBattle()`
  - `CreateUnitsFromDeck(List<CardInstance> _deck, BattleContext _ctx)`（`private`）
  - `ResetUnitsTurnFlags()`（`private`）
  - `GetActiveUnit(BattleSide _side, int _index)`
  - `IsAllActiveUnitsActedThisTurn(BattleSide _side)`
  - `EndCurrentSideTurn(bool _auto)`
  - `CheckBattleEnd()`（`private`）
  - `HasAllDead(List<CardUnit> _units)`（`private`）
  - `GetUnitSafe(List<CardUnit> _list, int _index)`（`private static`）
  - `GetPlayerActiveArray(BattleSide _side)`
  - `SwitchActiveWithBench(BattleSide _side, int _slotIdx, CardUnit _benchUnit)`
  - `BuildListFromActive(CardUnit[] _active)`（`private static`）

---

### `Core/Battle/BattleContext.cs`

- **`BattleContext`**
  - `BattleContext()`（无参构造）
  - `BattleContext(bool _isPVP, List<CardInstance> _playerDeck, List<CardInstance> _enemyDeck)`（构造）

---

### `Core/Battle/CardUnit.cs`

- **`CardUnit`**
  - `TakeDamage(int _damage)`
  - `Heal(int _amount)`
  - `CardUnit(CardInstance _card, BattleContext _ctx)`（构造）

---

### `Core/Battle/BattleFieldView.cs`

- **`BattleFieldView`**
  - `SetUpCards(IReadOnlyList<CardUnit> _player1Units, IReadOnlyList<CardUnit> _player2Units)`
  - `InstantiatePrefabs(...)`（`private`）
  - `RefreshAllCards()`
  - `ClearCards()`
  - `ClearContainer(Transform _container)`（`private static`）

---

### `Core/Card/CardData.cs`

- **`CardData`**
  - `GetBaseAttack()`
  - `GetBaseHealth()`

---

### `Core/Card/CardEffect.cs`

- **`CardEffect`**
  - `Excute()`（`public abstract`）

---

### `Core/Card/CardInstance.cs`

- **`CardInstance`**
  - `CardInstance(string _cardId)`（构造）
  - `CardInstance(CardData _cardData)`（构造）
  - `CreateNew(string _cardId)`（`public static`）

---

### `Core/Card/CardStatsService.cs`

- **`CardStatsService`**
  - `GetAttack(CardInstance _instance, CardData _data, bool _useEnhancement)`（`public static`）
  - `GetHealth(CardInstance _instance, CardData _data, bool _useEnhancement)`（`public static`）
  - `GetCost(CardData _data)`（`public static`）

---

### `Core/Card/CardUI.cs`

- **`CardUI`**
  - `SetUIPanelManager(UIPanelManager _mgr)`
  - `OnPointerClick(PointerEventData eventData)`（`public`，接口实现）
  - `Bind(CardUnit _unit)`
  - `RefreshDisplay()`
  - `GetBoundUnit()`

---

### `UI/BaseUIPanel.cs`

- **`UIPanelNames`**（`static class`）
  - （仅字符串常量，无方法）
- **`BaseUIPanel`**
  - `Awake()`（`protected virtual`）
  - `OnDestroy()`（`private`）
  - `RegisterPanel()`（`public virtual`）
  - `UnRegisterPanel()`
  - `ShowPanel()`（`public virtual`）
  - `HidePanel()`（`public virtual`）
  - `OnShow()`（`public virtual`）
  - `OnHide()`（`public virtual`）

---

### `UI/TitleScene/TitlePanel.cs`

- **`TitlePanel`**
  - `OnClickLogin()`
  - `OnClickLogout()`
  - `OnClickSetting()`
  - `OnClickExit()`

---

### `UI/TitleScene/LoginPanel.cs`

- **`LoginPanel`**
  - `Start()`（`private`）
  - `OnClickLogin()`
  - `OnClickRegister()`
  - `OnClickCancel()`

---

### `UI/TitleScene/SaveSelectPanel.cs`

- **`SaveSelectPanel`**
  - `Start()`（`private`）
  - `OnClickSlot(int _slotIndex)`（`private`）

---

### `UI/BattleScene/BattlePanel.cs`

- **`BattlePanel`**
  - `Awake()`（`protected override`）
  - `OnClickEndTurnButton()`

---

### `UI/BattleScene/CardActionPanel.cs`

- **`CardActionPanel`**
  - `Awake()`（`protected override`）
  - `OnEnable()`（`private`）
  - `OnDisable()`（`private`）
  - `OnClickSkillBtn()`
  - `OnClickChangeCardBtn()`
  - `OnClickEmojiBtn()`
  - `OnClickGiveUpBtn()`

---

### `UI/BattleScene/CardSelectPanel.cs`

- **`CardSelectPanel`**
  - `Awake()`（`protected override`）
  - `ShowSwitchMode(BattleSide _side, int _slotidx)`
  - `OnClickBenchCard(CardUnit _benchUnit)`（`private`）
  - `GetBenchUnits(...)`（`private static`）
  - `ShowForUnits(List<CardUnit> _units)`
  - `RebuildList(List<CardUnit> _units)`（`private`）
  - `OnClickItemIndex(int _index)`（`private`）
  - `RefreshSelectedUI()`
  - `OnClickConfirm()`（`private`）
  - `OnClickCancle()`（`private`）
  - `ClearSpawned()`（`private`）

---

### `UI/BattleScene/CardSelectItemView.cs`

- **`CardSelectItemView`**
  - `Bind(int _index, CardUnit _unit, bool _selected, Action<int> _onClickIndex)`
  - `BindBench(CardUnit _unit, Action<CardUnit> _onPick)`
  - `SetSelected(bool _selected)`

---

## 3. 缩进大纲（可整段复制到 Xmind「大纲」或作为节点参考）

```
Assets/Scripts
  Common
    Singleton.cs
      Singleton<T>
        Awake
        OnDestroy
  Test
    TestBattleScene.cs
      TestBattleScene
        Start
        OnClickStartBattleButton
  Services
    AccountEntry.cs
      AccountEntry
        AccountEntry()
        AccountEntry(string, string)
      AccountListData
    SaveManager.cs
      SaveManager
        GetSavePath
        Awake
        Load
        Save
        AddCardToInventory
        FindInstanceById
        GetCurrentDeck
        SetCurrentDeckIndex
        AddDeck
        RemoveDeckAt
        SetDeckInstanceIds
        CreateDefaultSaveData
        SelectSlot
        HasSaveInSlot
        GetSlotOccupied
        GetCurrentSlotIndex
        GetCurrentAccountSaveCount
        CreateDefaultSaveInSlot
    SaveData.cs
      SaveData
        SaveData()
      DeckSlot
        DeckSlot()
        DeckSlot(string)
    AccountManager.cs
      AccountManager
        Awake
        LoadAccounts
        SaveAccounts
        Register
        Login
        Logout
        GetCurrentAccountId
        GetAccountNum
        IsLoggedIn
    CardDataBase.cs
      CardDataBase
        Awake
        GetCardData
        RegisterCardData
    UIPanelManager.cs
      UIPanelManager
        RegisterPanel
        UnRegisterPanel
        ShowPanel
        HideAllPanels
        PushPanel
        Back
        ReplacePanel
        ClearStackAndShow
        GetPanel
    SceneManagers
      TitleSceneManager.cs
        TitleSceneManager
          Start
      BattleSceneManager.cs
        BattleSceneManager
          Start
  Core
    Battle
      BattleManager.cs
        BattleSide (enum)
          Player1
          Player2
        BattleManager
          StartBattlePrepare
          ConfirimPLayer1Actives
          AutoSelectPlayer2Actives
          StartBattle
          CreateUnitsFromDeck
          ResetUnitsTurnFlags
          GetActiveUnit
          IsAllActiveUnitsActedThisTurn
          EndCurrentSideTurn
          CheckBattleEnd
          HasAllDead
          GetUnitSafe
          GetPlayerActiveArray
          SwitchActiveWithBench
          BuildListFromActive
      BattleContext.cs
        BattleContext
          BattleContext()
          BattleContext(bool, List, List)
      CardUnit.cs
        CardUnit
          TakeDamage
          Heal
          CardUnit(CardInstance, BattleContext)
      BattleFieldView.cs
        BattleFieldView
          SetUpCards
          InstantiatePrefabs
          RefreshAllCards
          ClearCards
          ClearContainer
    Card
      CardData.cs
        CardData
          GetBaseAttack
          GetBaseHealth
      CardEffect.cs
        CardEffect
          Excute
      CardInstance.cs
        CardInstance
          CardInstance(string)
          CardInstance(CardData)
          CreateNew
      CardStatsService.cs
        CardStatsService
          GetAttack
          GetHealth
          GetCost
      CardUI.cs
        CardUI
          SetUIPanelManager
          OnPointerClick
          Bind
          RefreshDisplay
          GetBoundUnit
  UI
    BaseUIPanel.cs
      UIPanelNames
      BaseUIPanel
        Awake
        OnDestroy
        RegisterPanel
        UnRegisterPanel
        ShowPanel
        HidePanel
        OnShow
        OnHide
    TitleScene
      TitlePanel.cs
        TitlePanel
          OnClickLogin
          OnClickLogout
          OnClickSetting
          OnClickExit
      LoginPanel.cs
        LoginPanel
          Start
          OnClickLogin
          OnClickRegister
          OnClickCancel
      SaveSelectPanel.cs
        SaveSelectPanel
          Start
          OnClickSlot
    BattleScene
      BattlePanel.cs
        BattlePanel
          Awake
          OnClickEndTurnButton
      CardActionPanel.cs
        CardActionPanel
          Awake
          OnEnable
          OnDisable
          OnClickSkillBtn
          OnClickChangeCardBtn
          OnClickEmojiBtn
          OnClickGiveUpBtn
      CardSelectPanel.cs
        CardSelectPanel
          Awake
          ShowSwitchMode
          OnClickBenchCard
          GetBenchUnits
          ShowForUnits
          RebuildList
          OnClickItemIndex
          RefreshSelectedUI
          OnClickConfirm
          OnClickCancle
          ClearSpawned
      CardSelectItemView.cs
        CardSelectItemView
          Bind
          BindBench
          SetSelected
```

---

*生成日期：2026-03-22。脚本路径：`Documentation/_gen_cs_outline.py`（需 Python 3）。*
