# 工作目的 (2026-03-17)

> 说明本轮「为什么做这些事」。改动列表见 `2026-03-17_项目改动清单.md`，代码细节见 `2026-03-17_代码改动说明.md`。

## 2026-03-17 工作目的

1. **文档与协作**：为 `Assets/Scripts` 下脚本补充**简体中文 XML 文档注释**（`/// <summary>`），便于新对话与团队成员快速理解职责；修复部分文件因编码导致的**乱码**，统一以 UTF-8 保存。
2. **战斗流程扩展**：在 **2026-03-13 回合制与选卡框架** 基础上，落实 **准备阶段选两张上场 → 确认后生成预制体 → 正式开战**；支持 **替补切换**（仅显示未上场卡、点选替换并刷新战场）。
3. **降低 UI 与单例硬耦合**：在不大改架构的前提下，通过 **`BaseUIPanel` 与 `BattleFieldView` 的序列化注入** + **`CardUI.SetUIPanelManager`**，减少面板与卡牌 UI 中直接写 `UIPanelManager.Instance` 的依赖（保留回退以便迁移）。
4. **规则沉淀**：新增 Cursor 规则 **「仅回答」** 与 **「结构与设计优先」**，约束后续对话行为与编码习惯。
5. **工程卫生**：修正 `CardDataBase` 错误日志标签、`BattleFieldView` 清理子物体方式等易引发运行时问题的小点。

---

*与 2026-03-17 总结文档配套。*
