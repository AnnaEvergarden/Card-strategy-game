---
description: 总结与存储流程（何时总结、存哪、对话开始是否读、怎么写 AI_SUMMARY / USER_SUMMARY）
alwaysApply: true
---

# 总结与存储（合并规范）

## 何时执行总结与存储

在以下任一情况执行总结与存储流程：

- 对话轮数较多时；
- 用户明确说「请总结并保存」「快到头了」「上下文太长了」时；
- 对话接近 token 上限时。

执行内容（对话接近 token 上限时须自动执行）：

1. 生成 **AI_SUMMARY**：技术总结，记录本次对话中的代码修改、架构变更、已解决问题等。
2. 生成 **USER_SUMMARY**：面向用户的总结，用简洁中文说明本次完成的功能和遗留问题；**开头必须有「对话内容总结」**（见下文 USER_SUMMARY 规范）。
3. 在 USER_SUMMARY（或 AI_SUMMARY）末尾提示用户可**开启新一轮对话**，以便继续开发。

参考格式：与项目 `Documentation/` 中已有 `*_AI_SUMMARY.md`、`*_USER_SUMMARY.md` 保持一致（若存在 `2026-02-09_*` 等示例亦可对照；无固定日期文件时以**最新日期**的总结结构为准）。

## 存放位置与文件名

- 固定目录：`Documentation/`。
- 文件名格式：`YYYY-MM-DD_AI_SUMMARY.md`、`YYYY-MM-DD_USER_SUMMARY.md`（可带会话 ID 区分多轮）。
- **可生成多个文档区分总结内容**：除上述两份外，可按内容拆分为多份文档便于查看区别，例如：
  - `YYYY-MM-DD_项目改动清单.md`：仅列增/删/改；
  - `YYYY-MM-DD_工作目的.md`：仅写本轮工作目的；
  - `YYYY-MM-DD_代码改动说明.md`：仅写代码改动的逐一说明（作用、改动前、改动后、目的）。
  对话开始时仍以「最近日期的 `*_AI_SUMMARY.md`」为主恢复上下文；其他文档供按需查阅。

## 对话开始时

- 先检查 `Documentation/`（或 `.cursor/session/`）下是否存在最近的 `*_AI_SUMMARY.md`。
- 若存在，先读取该文件，将其中「项目状态、已做修改、技术决策、待办」作为本对话的初始上下文，再回复用户第一条消息。
- 可选：同时列出 USER_SUMMARY 中的待办，问用户是否从某一条继续。

---

## 如何书写总结

总结文档必须同时满足以下四项：

1. **对项目做出的改动**：明确列出增（新建文件/类型/方法）、删（移除的文件或逻辑）、改（修改过的文件与改动类型），便于版本对比与回溯。
2. **这轮工作的目的**：说明本轮目标（例如：新建某脚本是为了支持多账号存档、修改某类是为了修复槽位与卡组下标混用等），让后续对话或新成员理解「为什么这样改」。
3. **代码改动的逐一说明**：若有代码改动，对每处改动写清：**作用**（该段代码负责什么）、**改动前后变化**（改前行为/代码要点 → 改后行为/代码要点）、**目的**（为何要这样改）。
4. **可生成多个文档区分总结内容**：除 AI_SUMMARY / USER_SUMMARY 外，可单独生成「项目改动清单」「工作目的」「代码改动说明」等文档，方便按需查看区别，避免单文件过长。

### AI_SUMMARY（给 AI 恢复上下文）

结构保持：项目概述、架构/类的功能介绍、技术决策、**项目改动清单（增/删/改）**、**本轮工作目的**、已做修改（含代码改动：作用/前后变化/目的）、已知问题、待办。

### USER_SUMMARY（给用户看）

文件**最上方**必须先写 **「对话内容总结」**，再写 **「本轮做了什么」** 等后续小节。

#### 对话内容总结（必填，置顶）

用一小段话 + 若干要点，让读者不往下翻也能了解本轮对话全貌：

- **谈了什么**：本轮对话的主题与范围（例如：存档架构、战斗 UI、分层与事件边界等）。
- **讨论了哪些问题**：用户提出的具体问题、取舍或疑点（可列条）。
- **做了什么事**：结合**代码与仓库变更**概括——新增/修改/删除了哪些文件或规则、**为了**实现什么功能、修复什么 bug、或优化什么结构；若本轮**无代码改动**，写明「仅讨论 / 仅答疑」即可。
- **最终怎么样**：是否已落地、是否仍待合并、遗留风险或待办一句话收尾。

#### 「本轮做了什么」结构（紧随对话内容总结之后）

1. **项目改动概览**：用列表或表格简要列出本轮的**增 / 删 / 改**（文件路径、改动类型）。
2. **本轮工作目的**：一两句话说明本轮目标（如：支持多账号与多槽位存档、统一日志与中文格式、修复槽位与卡组下标混用等）。
3. **若有代码改动，则逐一介绍**：每个改动点包含：
   - **作用**：该处代码/逻辑负责什么；
   - **改动前**：原行为或原代码要点；
   - **改动后**：现行为或现代码要点；
   - **目的**：为何要做这一改。
4. 非代码改动（如新增规则、文档）：说明新建/修改了哪些文件、用途与要求。
5. 文末（或 AI_SUMMARY 文末）：**提示可开启新对话**以继续开发。

- 避免仅写「梳理了项目」「修复了若干问题」等笼统描述；即使改动少也按上述结构逐条写出。

示例（含 **对话内容总结** + 目的 + 改动概览 + 代码逐一说明）：

```markdown
## 对话内容总结

本轮我们讨论了多槽位存档与账号切换的设计，澄清了槽位下标与卡组下标混用的问题；在代码侧修改了 `SaveManager`、`AccountManager` 等以分离槽位与卡组，并新增中文日志格式规则。**结果**：存档路径已按槽位区分，待你在真机上验证多账号切换。

---

## 本轮做了什么

### 项目改动概览（增/删/改）
- **增**：`Documentation/2025-03-08_AI_SUMMARY.md`，`.cursor/rules/code-chinese-format.mdc`
- **改**：`SaveManager.cs`（存档路径与槽位逻辑）、`AccountManager.cs`（Awake/IsLoggedIn/Register）、`GameBootstrap.cs`（日志前缀与空检查）、`BattleManager.cs`（日志与笔误）

### 本轮工作目的
支持按账号+槽位存档、统一调试日志与中文格式便于排查与协作，修复槽位与当前卡组下标混用导致的存读错槽位问题。

### 代码改动逐一说明
#### SaveManager：存档路径与 SelectSlot
- **作用**：决定存档文件路径与切换槽位时的行为。
- **改动前**：`SavePath` 使用 `current.currentDeckIndex`，SelectSlot 改的是 `current.currentDeckIndex`，槽位与卡组下标混用。
- **改动后**：`SavePath` 使用 `currentSlotIndex`，SelectSlot 设置 `currentSlotIndex` 后 Load；槽位边界改为 `>= SaveSlotCount`。
- **目的**：槽位仅表示存档槽位，卡组下标仅表示当前选中的卡组，二者分离避免存读错文件。
...
```

参考格式可与项目 `Documentation/` 中已有 `*_AI_SUMMARY.md`、`*_USER_SUMMARY.md` 保持一致；完整范例亦可对照 `2026-02-09_AI_SUMMARY.md` / `2026-02-09_USER_SUMMARY.md`（若仓库中已存在）。
