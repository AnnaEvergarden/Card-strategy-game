---
description: 代码中中文书写规范（注释、字符串、日志等须用可正确编译/显示的简体中文）
alwaysApply: true
---

# 代码中的中文格式规范

## 适用范围

本规范适用于**所有脚本（.cs）及配置中的中文**，包括但不限于：

- 注释：`//`、`/// <summary>`、`/* */`
- 特性中的字符串：`[Header("...")]`、`[Tooltip("...")]`
- 代码中的字符串与插值：`"..."`、`$"..."`、`Debug.Log` / `LogWarning` / `LogError` 中的中文
- 序列化字段的默认值、Inspector 显示名等

## 格式要求

1. **编码与字体**
   - 一律使用 **UTF-8 编码** 的 **简体中文**。
   - 须保证在 **Visual Studio / 团结编辑器** 中能正确编译、无乱码、无问号或替代字符。

2. **用词与风格**
   - 与项目内 `SaveManager.cs` 等已正常显示的脚本保持一致风格。
   - 常用词示例：**负责**、**当前**、**没有**、**加载**、**存档**、**路径**、**创建**、**默认**、**背包**、**卡牌**、**卡组**、**序号**、**标准**、**为空**、**失败**、**该**、**若**、**则** 等。
   - 避免使用仅在特定环境下才能正确显示的字符或生僻字；遇专有名词（如 PVP、PVE、CardData）可中英混排。

3. **禁止**
   - 禁止出现成串 `?`、`�`、或明显乱码字符。
   - 禁止使用 GB2312/GBK 等非 UTF-8 编码保存后在其他环境打开导致乱码的写法。

## 参考示例（与 SaveManager 一致）

```csharp
/// <summary> 当前运行时存档数据，Load后或默认数据 </summary>
Debug.LogWarning($"[SaveManager] 当前路径没有存档文件 => {SavePath}");
Debug.Log($"[SaveManager] 加载数据来自{SavePath}");
// 依赖 AccountManager提供当前账号
[Header("引用（若为空则尝试 FindObjectOfType）")]
```

新增或修改含中文的代码时，按上述格式书写，并确保文件以 **UTF-8** 保存。
