---
description: 增改代码前先考虑结构、设计模式、复用与可扩展性等专业设计因素
alwaysApply: true
---

# 结构与设计优先（增改代码前）

## 原则

在**添加或修改代码**之前，优先从工程化角度想清楚再动手，避免只为“能跑”而堆逻辑。

## 动手前应简要考虑

1. **结构与分层**  
   - 新逻辑应落在哪一层（数据 / 领域 / 应用 / 表现 / 基础设施）？  
   - 是否与现有 `Core`、`Services`、`UI` 等划分一致？依赖方向是否由外向内、避免循环引用。

2. **设计模式与惯用法（按需选用，勿滥用）**  
   - 适合时用：**策略**（多规则切换）、**状态**（流程/回合）、**观察者/事件**（UI 与逻辑解耦）、**工厂**（创建复杂对象）、**单例**（全局管理器，已有则复用约定）、**命令**（撤销/回放）等。  
   - 说明**为何**选用某种模式或为何不用（过度设计时明确写“保持简单”的理由）。

3. **复用性**  
   - 是否存在重复代码可抽成服务类、静态工具、基类或组件？  
   - 配置与逻辑是否分离（ScriptableObject、表驱动、常量类）。

4. **可扩展性**  
   - 未来新增卡牌效果、新面板、新战斗规则时，改动面是否局部？  
   - 是否用接口/抽象类/事件扩展点，而非到处改 `switch` 或硬编码。

5. **权衡**  
   - 在**简单可维护**与**抽象灵活**之间做显式取舍；小功能可 YAGNI，复杂域再引入抽象。

## 回复与实现时的建议格式

- 复杂改动时：先给**简短设计说明**（1～5 点），再写或改代码。  
- 若用户只要快速补丁：仍可在注释或回复里点明后续可如何重构，避免技术债无声累积。
