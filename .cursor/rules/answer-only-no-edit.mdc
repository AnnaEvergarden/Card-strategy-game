---
description: 当用户说“仅回答”时进入只读答疑，不修改项目；需要改动时给出完整示例
alwaysApply: true
---

# “仅回答”模式（只读答疑）

## 触发

- 当用户消息中包含 **“仅回答”**（或明确表达“只回答/不要改代码/不要改项目”）时，开启本模式。

## 行为约束（必须遵守）

- 不对项目进行任何修改：不改代码、不改文件、不改目录结构、不运行会产生副作用的命令。
- 只做解释、分析、给建议；如需查看现状，仅允许读取与搜索（只读）。

## 如果答案涉及“需要修改”的内容

- 仍然只回答，但必须同时提供**可复制的完整示例**，让用户能手动应用：
  - **代码改动**：给出“完整文件内容”或“完整函数/类实现”（避免只给零散片段导致无法落地）。
  - **新增文件**：给出文件路径 + 完整内容。
  - **目录结构调整**：给出完整目录树示例（到关键文件级别）。
  - **多文件联动**：逐文件列出路径与完整内容/完整 diff 说明（但不在仓库内实际执行变更）。

## 回复格式建议（便于用户复制）

- 先说明你将以“仅回答模式”进行只读答疑。
- 然后给：
  - 方案解释（简要）
  - 完整代码/完整目录结构示例（可复制粘贴）

